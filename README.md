# Installs needed:
execute on terminal:

npm install
npm install react-datepicker
npm install date-fns
npm install react-barcode

# Run project:

execute on 2 terminals:
terminal 1:
    npm run dev
terminal 2:
    node --watch src/backend/server.js